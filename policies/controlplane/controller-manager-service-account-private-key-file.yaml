apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: controlplane.controller-manager.service-account-private-key-file
  annotations:
    audit.k8s-auditor.io/title: "kube-controller-manager has no explicit --service-account-private-key-file"
    audit.k8s-auditor.io/severity: high
    audit.k8s-auditor.io/category: control-plane-config
    audit.k8s-auditor.io/remediation: >-
      Set --service-account-private-key-file to the private key used to sign ServiceAccount tokens.
    audit.k8s-auditor.io/cis: "1.3.4"
spec:
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
      - apiGroups: [""]
        apiVersions: ["v1"]
        operations: ["CREATE", "UPDATE"]
        resources: ["pods"]
  validations:
    - expression: |-
        !((has(object.metadata.labels) && 'component' in object.metadata.labels && object.metadata.labels['component'] == 'kube-controller-manager') || (object.metadata.namespace == 'kube-system' && object.metadata.name.startsWith('kube-controller-manager'))) || (
          object.spec.containers.exists(c, ((has(c.command) ? c.command : []) + (has(c.args) ? c.args : [])).exists(a, a.startsWith('--service-account-private-key-file=')))
        )
      message: "[indirect signal — inferred from the kube-controller-manager static Pod's command-line flags via the Kubernetes API, not confirmed via node/file access] --service-account-private-key-file is not set."

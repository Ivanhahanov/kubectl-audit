apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: scoped
  namespace: payments
spec:
  action: ALLOW
  rules:
    - from:
        - source:
            principals: ["cluster.local/ns/frontend/sa/web"]
      to:
        - operation:
            paths: ["/health", "/ready"]

apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: gw2
spec:
  servers:
    - port:
        number: 443
        name: https
        protocol: HTTPS
      tls:
        mode: SIMPLE
        minProtocolVersion: TLSV1_2
      hosts:
        - "*.example.com"
